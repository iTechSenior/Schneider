// const { SCREEN_MAIN } from '../const/Home'

const INITIAL_STATE = {
  search_text: '',
  search_bool: false
};

const Home = (state = INITIAL_STATE, action) => {
  switch(action.type){

    default:
      return state;
  }
}

export default Home;
